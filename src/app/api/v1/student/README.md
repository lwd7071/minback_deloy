# Student API boundary

Dev B sở hữu Student auth và notification API. Dev A sở hữu Student Profile API. Student identity luôn lấy từ `StudentSession`, không nhận `studentId` từ browser để quyết định quyền.
