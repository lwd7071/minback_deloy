# UI modules

UI modules are organized domain-first, with `teacher` and `student` subdirectories where a domain has role-specific presentation. Shared visual primitives remain in `ui/`; application routes remain in `src/app/`.
